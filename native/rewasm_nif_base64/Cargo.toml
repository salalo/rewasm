[package]
name = "rewasm_nif_base64"
version = "0.1.0"
authors = []
edition = "2018"

[lib]
name = "rewasm_nif_base64"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.23.0"
base64 = "0.13.0"
